import { MessageRatePipe } from './message-rate.pipe';

describe('MessageRatePipe', () => {
  it('create an instance', () => {
    const pipe = new MessageRatePipe();
    expect(pipe).toBeTruthy();
  });
});
